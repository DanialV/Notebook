module.exports = function(req,permission){
  if (req.user == 'undefined') {
      console.mongo('Security', 'Access Denied ' + req.url);
      res.status(500).json({
          'status': 403
      });
      return true;
  }
  let permissions = req.user.permissions;
  if (permissions.indexOf(permission) == -1) {
      console.mongo('Security', 'User: ' + req.username + 'Access Denied ' + req.url);
       res.status(500).json({
          'status': 403
      });
      return true;
  }
  return false;

}
